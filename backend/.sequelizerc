const path = require('path');

// Keeps Sequelize CLI pointed at the same layout the runtime uses.
module.exports = {
  config: path.resolve(__dirname, 'src/config/sequelize.js'),
  'models-path': path.resolve(__dirname, 'src/models'),
  'migrations-path': path.resolve(__dirname, 'src/db/migrations'),
  'seeders-path': path.resolve(__dirname, 'src/db/seeders'),
};
